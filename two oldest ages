//return the two "oldest" ages from an array
// I returned the first two numbers of a reverse sort

function twoOldestAges(ages){
  return ages.sort((x,y) => {return x - y}).slice(ages.length - 2, ages.length);

}

}
